═══════════════════════════════════════════════════════════════════════════════
                    MARKET PRICE COMPARISON SYSTEM
                         KALAN HATALAR VE SORUNLAR
═══════════════════════════════════════════════════════════════════════════════

Son Güncelleme: 2025-10-23
Kritik Hatalar: ✅ Tamamlandı
Kalan Sorunlar: 16 adet

═══════════════════════════════════════════════════════════════════════════════
⚠️  TASARIM PROBLEMLERİ
═══════════════════════════════════════════════════════════════════════════════

1. PRICE ENTITY BASEENTITY'DEN TÜREMİYOR ✅ Tamamlandı
   Dosya: src/Domain/Entities/Price.cs:3-5
   Problem: Diğer tüm entity'ler BaseEntity'den türerken Price türemiyor
   - PriceId kullanılıyor (tutarlılık için Id olmalı)
   - IsDeleted ve CreatedAt property'leri eksik
   - Soft delete desteği yok

   Çözüm: Price class'ını BaseEntity'den türet
   Öncelik: ORTA

───────────────────────────────────────────────────────────────────────────────

2. SOFT DELETE INCONSISTENCY ✅ Tamamlandı
   Dosya: src/DataAccess/Data/AppDbContext.cs:133-134
   Problem: Sadece User ve UserAddress için query filter var
   - Product, Category, Market, ShoppingList için eksik
   - Soft delete mekanizması tutarsız

   Çözüm: Tüm BaseEntity'den türeyen sınıflar için global query filter ekle
   Öncelik: ORTA

───────────────────────────────────────────────────────────────────────────────

3. NULLABLE NAVIGATION PROPERTY UYUMSUZLUĞU ✅ Tamamlandı
   Dosya: Çeşitli entity dosyaları
   Problem: Navigation property'ler null! ile init edilmiş
   - Foreign key'lerde cascade delete stratejisi yok
   - OnDelete davranışı belirtilmemiş

   Çözüm: AppDbContext'te cascade delete stratejileri tanımla
   Öncelik: ORTA

───────────────────────────────────────────────────────────────────────────────

4. VALIDATION ATTRIBUTES EKSİK ✅ Tamamlandı
   Dosya: src/Domain/Entities/*.cs
   Problem: Entity property'lerinde validation yok
   - [Required], [MaxLength] gibi attribute'lar eksik
   - Data annotations eksik
   - DTO'larda kısmi validation var ama entity'lerde yok

   Çözüm: Entity'lere data annotations ekle
   Öncelik: DÜŞÜK

───────────────────────────────────────────────────────────────────────────────

5. INDEX EKSİKLİKLERİ ✅ Tamamlandı
   Dosya: src/DataAccess/Data/AppDbContext.cs
   Problem: Sadece User.Email için unique index var
   - Performance için gerekli indexler eksik:
     * Product.CategoryId (Foreign Key Index)
     * Price.ProductId (Foreign Key Index)
     * Price.MarketId (Foreign Key Index)
     * ShoppingList.UserId (Foreign Key Index)
     * ShoppingListItem.ShoppingListId (Foreign Key Index)
     * Product.ProductName (Search Index)
     * Market.MarketName (Search Index)

   Çözüm: OnModelCreating'de HasIndex() ile indexler ekle
   Öncelik: ORTA

───────────────────────────────────────────────────────────────────────────────

6. EF CORE QUERY FILTER UYARISI ✅ Tamamlandı
   Durum: Migration sırasında uyarı alındı
   Problem: User entity'sinde global query filter var ve ShoppingList ile
            required relationship var. Filtered out olunca sorun çıkabilir.

   Çözüm: ShoppingList navigation'ını optional yap veya ShoppingList için
          de matching query filter tanımla
   Öncelik: ORTA

═══════════════════════════════════════════════════════════════════════════════
🔧 EKSİK FONKSİYONELLİK
═══════════════════════════════════════════════════════════════════════════════

7. REPOSITORY PATTERN EKSİKLİKLERİ
   Dosya: src/DataAccess/Repositories/
   Problem: Sadece UserRepository implement edilmiş

   Eksik Repository'ler:
   - IProductRepository / ProductRepository
   - ICategoryRepository / CategoryRepository
   - IMarketRepository / MarketRepository
   - IPriceRepository / PriceRepository
   - IShoppingListRepository / ShoppingListRepository
   - IUserAddressRepository / UserAddressRepository

   Öncelik: YÜKSEK

───────────────────────────────────────────────────────────────────────────────

8. SERVICE LAYER EKSİKLİKLERİ
   Dosya: src/Domain/Services/ & src/Domain/Interfaces/Services/
   Problem: Sadece UserService ve AuthService var

   Eksik Servisler:
   - IProductService / ProductService
   - ICategoryService / CategoryService
   - IMarketService / MarketService
   - IPriceService / PriceService
   - IShoppingListService / ShoppingListService
   - IUserAddressService / UserAddressService

   Öncelik: YÜKSEK

───────────────────────────────────────────────────────────────────────────────

9. CONTROLLER EKSİKLİKLERİ
   Dosya: src/API/Controllers/
   Problem: Sadece UsersController ve AuthController var

   Eksik Controller'lar:
   - ProductsController (GET, POST, PUT, DELETE)
   - CategoriesController
   - MarketsController
   - PricesController
   - ShoppingListsController
   - UserAddressesController

   Ayrıca UsersController'da eksik endpoint'ler:
   - POST /api/users (Create)
   - PUT /api/users/{id} (Update)
   - DELETE /api/users/{id} (Soft Delete)

   Öncelik: YÜKSEK

───────────────────────────────────────────────────────────────────────────────

10. DTO EKSİKLİKLERİ
    Dosya: src/DTOs/DTOs/
    Problem: Sadece UserDTO, LoginRequestDTO ve RegisterRequestDTO var

    Eksik DTO'lar:
    Request DTO'lar:
    - CreateProductRequestDTO
    - UpdateProductRequestDTO
    - CreateCategoryRequestDTO
    - CreateMarketRequestDTO
    - CreatePriceRequestDTO
    - CreateShoppingListRequestDTO
    - AddShoppingListItemRequestDTO

    Response DTO'lar:
    - ProductResponseDTO
    - CategoryResponseDTO
    - MarketResponseDTO
    - PriceResponseDTO
    - ShoppingListResponseDTO
    - ProductWithPricesResponseDTO
    - PriceComparisonResponseDTO

    Öncelik: YÜKSEK

═══════════════════════════════════════════════════════════════════════════════
⚡ PERFORMANS & GÜVENLİK
═══════════════════════════════════════════════════════════════════════════════

11. ERROR HANDLING MIDDLEWARE YOK
    Dosya: src/API/Middleware/ (klasör boş)
    Problem: Global exception handling yok

    Eksikler:
    - ExceptionHandlingMiddleware
    - ValidationExceptionMiddleware
    - Structured error responses yok
    - Try-catch blokları controller'larda yok

    Öncelik: YÜKSEK

───────────────────────────────────────────────────────────────────────────────

12. LOGGING EKSİK
    Problem: Structured logging yapılandırması yok

    Eksikler:
    - Request/Response logging yok
    - Error logging yok
    - Serilog veya NLog entegrasyonu yok
    - Log seviyeleri yapılandırılmamış

    Öncelik: ORTA

───────────────────────────────────────────────────────────────────────────────

13. RATE LIMITING YOK
    Dosya: src/API/Program.cs
    Problem: API rate limiting yapılandırması yok

    Riskler:
    - DDoS saldırılarına açık
    - Brute force login denemeleri engellenemiyor
    - Resource abuse mümkün

    Çözüm: AspNetCoreRateLimit veya .NET 7+ built-in rate limiting kullan
    Öncelik: YÜKSEK

───────────────────────────────────────────────────────────────────────────────

14. INPUT VALIDATION EKSİK
    Problem: Controller'larda input validation eksik

    Eksikler:
    - ModelState.IsValid kontrolleri bazı endpoint'lerde var bazılarında yok
    - Custom validation attribute'ları yok
    - FluentValidation kullanılmamış

    Öncelik: ORTA

───────────────────────────────────────────────────────────────────────────────

15. PASSWORD HASH ALGORİTMASI ZAYıF
    Dosya: src/Domain/Services/AuthService.cs:91-100
    Problem: SHA256 password hashing için uygun değil

    Riskler:
    - Salt kullanılmıyor
    - Rainbow table saldırılarına açık
    - Brute force için çok hızlı

    Çözüm: BCrypt, Argon2 veya PBKDF2 kullan
    Öncelik: KRİTİK (Güvenlik)

═══════════════════════════════════════════════════════════════════════════════
📊 EKSİK ÖZELLIKLER
═══════════════════════════════════════════════════════════════════════════════

16. HEALTH CHECK ENDPOINT YOK
    Problem: /health veya /healthz endpoint yok
    - Kubernetes/Docker health probe kullanamıyor
    - Database bağlantısı kontrolü yok

    Çözüm: Microsoft.Extensions.Diagnostics.HealthChecks kullan
    Öncelik: DÜŞÜK

───────────────────────────────────────────────────────────────────────────────

17. API VERSIONING YOK
    Problem: API versioning yapılandırması yok
    - Breaking change'lerde backward compatibility sorunu
    - /api/v1/, /api/v2/ gibi versiyonlama yok

    Çözüm: Microsoft.AspNetCore.Mvc.Versioning kullan
    Öncelik: DÜŞÜK

───────────────────────────────────────────────────────────────────────────────

18. CORS POLİCY GENİŞ
    Dosya: src/API/Program.cs:42-50
    Problem: AllowAnyOrigin() kullanılıyor

    Risk: Production'da tüm origin'lere açık

    Çözüm: Production için specific origin'ler tanımla:
    - Frontend URL'i
    - Mobile app domain'i

    Öncelik: YÜKSEK (Production öncesi)

═══════════════════════════════════════════════════════════════════════════════
📋 ÖNCELİK SIRASI
═══════════════════════════════════════════════════════════════════════════════

KRİTİK (Hemen):
[15] Password hash algoritması değiştirilmeli (BCrypt/Argon2)

YÜKSEK (Bu Sprint):
[7]  Repository pattern tamamlanmalı
[8]  Service layer tamamlanmalı
[9]  Controller'lar tamamlanmalı
[10] DTO'lar oluşturulmalı
[11] Error handling middleware eklenmeli
[13] Rate limiting eklenmeli
[18] CORS policy düzenlenmeli (Production öncesi)

ORTA (Sonraki Sprint):
[1]  Price entity BaseEntity'den türetilmeli
[2]  Soft delete tutarlılığı sağlanmalı
[3]  Cascade delete stratejileri eklenmeli
[5]  Index'ler eklenmeli
[6]  EF Core query filter uyarısı çözülmeli
[12] Logging yapılandırması eklenmeli
[14] Input validation güçlendirilmeli

DÜŞÜK (Backlog):
[4]  Validation attributes eklenmeli
[16] Health check endpoint eklenmeli
[17] API versioning eklenmeli

═══════════════════════════════════════════════════════════════════════════════
✅ TAMAMLANAN KRİTİK HATALAR
═══════════════════════════════════════════════════════════════════════════════

[✓] appsettings.Development.json oluşturuldu
[✓] JWT Authentication yapılandırması tamamlandı
[✓] Password hashing servisi implement edildi (SHA256 - değiştirilmesi öneriliyor)
[✓] Initial database migration oluşturuldu
[✓] CORS yapılandırması eklendi
[✓] Swagger JWT desteği eklendi

═══════════════════════════════════════════════════════════════════════════════
🚀 SONRAKI ADIMLAR
═══════════════════════════════════════════════════════════════════════════════

1. Password hashing algoritmasını BCrypt ile değiştir
2. Repository pattern'i tamamla (Product, Category, Market, Price, ShoppingList)
3. Service layer'ı tamamla
4. Controller'ları ve CRUD endpoint'leri oluştur
5. DTO'ları tamamla
6. Error handling middleware ekle
7. Rate limiting ekle
8. Unit test'ler yaz
9. Integration test'ler yaz
10. Database'i oluştur: dotnet ef database update

═══════════════════════════════════════════════════════════════════════════════